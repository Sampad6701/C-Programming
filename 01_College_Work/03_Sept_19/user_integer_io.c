// Integer Input and Output using user input

#include <stdio.h>
int main() {
    int isInteger;
    printf("Enter an integer: ");
    scanf("%d", &isInteger);
    printf("Number = %d\n", isInteger);
    return 0;
}