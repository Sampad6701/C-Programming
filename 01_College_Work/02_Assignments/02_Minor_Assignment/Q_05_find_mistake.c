#include <stdio.h>
int main()
{
    int i = 1;
    while ()
    {
        printf("%d", i++);
        if (i > 10)
            break;
    }
    return 0;
}

/*
while loop has no condition
error: expected expression before ‘)’ token
*/
